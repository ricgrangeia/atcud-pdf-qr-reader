networks:
  proxy-network:
    name: ${PROXY_NETWORK_NAME}
    external: true

services:
  app:
    image: go-api-app
    build:
      context: .
      dockerfile: Dockerfile
    container_name: go-atcud-pdf-qr-code-reader-app
    environment:
      - PORT=${PORT:-8080}
      - VLLM_BASE_URL=${VLLM_BASE_URL}
      - VLLM_API_KEY=${VLLM_API_KEY}
      - VLLM_MODEL=${VLLM_MODEL}
      - GIN_MODE=${GIN_MODE}
    # Sem ports: — o Traefik trata do roteamento externo.
    # Descomentar apenas para testes locais sem Traefik:
    # ports:
    #   - "8080:8080"
    init: true
    restart: unless-stopped
    user: "10001:10001"
    networks:
      - proxy-network
    labels:
      # Activar Traefik para este contentor
      - "traefik.enable=true"
      # Router HTTPS — serve domain com TLS
      - "traefik.http.routers.go-atcud-pdf-qr-code-reader-app.rule=Host(`${URL_HOST_DOMAIN}`)"
      - "traefik.http.routers.go-atcud-pdf-qr-code-reader-app.entrypoints=websecure"
      - "traefik.http.routers.go-atcud-pdf-qr-code-reader-app.tls=true"
      - "traefik.http.routers.go-atcud-pdf-qr-code-reader-app.tls.certresolver=letsencrypt"
      # Router HTTP — redireciona automaticamente para HTTPS
      - "traefik.http.routers.go-atcud-pdf-qr-code-reader-app-http.rule=Host(`${URL_HOST_DOMAIN}`)"
      - "traefik.http.routers.go-atcud-pdf-qr-code-reader-app-http.entrypoints=web"
      - "traefik.http.routers.go-atcud-pdf-qr-code-reader-app-http.middlewares=redirect-https"
      # Middleware de redireccionamento HTTP → HTTPS
      - "traefik.http.middlewares.redirect-https.redirectscheme.scheme=https"
      - "traefik.http.middlewares.redirect-https.redirectscheme.permanent=true"
      # Porta interna do contentor (deve coincidir com PORT)
      - "traefik.http.services.go-atcud-pdf-qr-code-reader-app.loadbalancer.server.port=8080"
    logging:
      driver: "json-file"
      options:
        max-size: "50m"
        max-file: "5"
